apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coin98-fe.fullname" . }}-test-connection"
  labels:
    {{- include "coin98-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coin98-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
